# Validation — 2026-09-17

Component/test revision: `a86127f` (`codex/protocol-v1-v2`). Documentation is
recorded in the following commit. This record distinguishes software validation
of the refactor from earlier hardware evidence.

## Hardware evidence

The owner confirmed that **V2 is fully working on a wall PCB marked V2.1** and
provided identification photographs for V2.1 and V1.0-T. This confirmation concerns
the established V2 protocol support; it is not a new bench test of this refactor.

The earlier [integrated V1 record](2026-09-16.md) demonstrates one query/OFF/ON
cycle on commit `6c4c093`. V1 wall LED staleness and intermittent physical-button
response after external traffic remain unresolved. This pass did not flash or
operate the deployed controller.

## Software checks

| Check | Result |
|---|---|
| Production C++ parsers/schedulers, host build with AddressSanitizer and UndefinedBehaviorSanitizer | Pass; seven named groups |
| Capture replay | Pass; 97 records (14 V1 button exchanges, 83 V2 request/status pairs) |
| Fixture provenance | Pass; original bytes and timestamps match every cited source line |
| ESPHome 2026.8.2 configuration tests | Pass; seven Python test methods with protocol/error subcases |
| ESPHome 2026.9.0 configuration tests | Pass; same suite |
| ESPHome 2026.8.2 ESP32-S3/ESP-IDF 5.5.5 compile | Pass; V1 and default V2, all entity platforms/new diagnostics |
| ESPHome 2026.9.0 ESP32-S3/ESP-IDF 5.5.5 compile | Pass; V1 and default V2, all entity platforms/new diagnostics |
| Public configuration examples | Both fragments and complete XIAO YAML validate with dummy credentials/local component source |
| Python formatting/lint, local documentation/image links and Git whitespace | Pass |

Host cases cover passive startup/poll/refresh/write suppression with DE staying
LOW, fragmented/bad/truncated frames, query-before-toggle, no-op requests,
missing replies without blind retry, concurrent requests, spill guard, state-age
expiry, clock wrap/reboot interruption, CRC/node learning, sequence mismatch and
node change. Only the V1 hardware transaction boundary is substituted in host
tests. Real direct-FIFO transport is compiled in the ESP32 builds.

These checks do not establish electrical timing, response ownership during bus
collisions, physical airflow, or wall-panel coexistence. Bench validation of this
refactor remains the next release step. No open-ended compatibility with future
ESPHome versions is claimed. CI and a combined check runner were outside scope.
